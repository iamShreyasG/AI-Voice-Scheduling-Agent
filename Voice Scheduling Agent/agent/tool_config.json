{
  "tool_name": "create_calendar_event",
  "type": "http",
  "method": "POST",
  "endpoint": "/create-event",
  "request_body": {
    "name": "string",
    "date": "string",
    "time": "string",
    "title": "string"
  },
  "description": "Creates a Google Calendar event after the user confirms all meeting details.",
  "notes": [
    "This tool is triggered only after explicit user confirmation.",
    "The agent accepts natural language date and time and normalizes internally.",
    "The backend uses Google Calendar API with a service account."
  ]
}
